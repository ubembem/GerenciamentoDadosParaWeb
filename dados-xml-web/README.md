# dados-xml-web

Aplicação Spring Boot (Java 11) com upload de XML, parsing via JAXB e persistência em H2. 
Telas: /upload, /consultar-dados, CRUDs de Produto e Cliente.

## Como executar
```bash
mvn spring-boot:run
```
Acesse http://localhost:8080/upload
